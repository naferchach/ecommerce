import Layout from '../components/Layout/Layout';
import Cart from '../components/Cart';

const CartPage = () => {
  return (
    <Layout>
      <Cart />
    </Layout>
  );
};

export default CartPage;
